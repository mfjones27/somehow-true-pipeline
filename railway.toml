[deploy]
startCommand = "python app.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Runtime defaults are in Dockerfile/app.py. Set API_TOKEN and keep
# PIPELINE_ENABLED=false in Railway service Variables; no cron command.
